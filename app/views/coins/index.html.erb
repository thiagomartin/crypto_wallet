<p style="color: green"><%= notice %></p>

<%= data_br(Date.today) %> </p>
<h1>Coins</h1>
<h2><%= cookies[:curso] %></h2>
<h2><%= session[:curso] %></h2>

<div id="coins">
  <% @coins.each do |coin| %>
    <%= render coin %>
    <p>
      <%= link_to t('links.show'), coin %>
    </p>
  <% end %>
</div>


<ul>
<li>
  <%= link_to t('links.cadaster'), new_coin_path %>
</li>
<li>
  <%= link_to t('links.back'), welcome_index_path %>
</li>
</ul>
